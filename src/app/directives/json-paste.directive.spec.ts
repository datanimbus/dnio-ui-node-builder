import { JsonPasteDirective } from './json-paste.directive';

describe('JsonPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonPasteDirective();
    expect(directive).toBeTruthy();
  });
});
