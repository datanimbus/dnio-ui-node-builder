import { JsonFileSelectDirective } from './json-file-select.directive';

describe('JsonFileSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonFileSelectDirective();
    expect(directive).toBeTruthy();
  });
});
